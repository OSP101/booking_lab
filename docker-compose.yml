version: '3'
services:
  booking-service:
    container_name: booking-lab
    restart: always
    build: .
    ports:
      - "80:3000"
    environment:
      DATABASE_URL: "mysql://root:booking@admin@10.199.7.28:3306/booking_lab?schema=public"
    networks:
      - booking-network


networks:
  booking-network:
    driver: bridge